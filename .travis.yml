reference: http://www.objc.io/issue-6/travis-ci.html
language: objective-c
cache: bundler
script:
- cd Example && pod install && cd -
- xctool test -workspace Example/REVOCS.xcworkspace -scheme REVOCS -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO
before_install:
- gem install cocoapods
